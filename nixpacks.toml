[phases.setup]
nixPkgs = ["nodejs", "chromium", "glib", "nss", "expat", "dbus", "libxml2", "libxslt"]

[phases.install]
cmds = ["npm install"]

[variables]
PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
PUPPETEER_EXECUTABLE_PATH = "chromium"
